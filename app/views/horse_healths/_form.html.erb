<%= form_for(@horse_health) do |f| %>
  <% if @horse_health.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@horse_health.errors.count, "error") %> prohibited this horse_health from being saved:</h2>

      <ul>
      <% @horse_health.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
