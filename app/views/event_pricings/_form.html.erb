<%= form_for(@event_pricing) do |f| %>
  <% if @event_pricing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_pricing.errors.count, "error") %> prohibited this event_pricing from being saved:</h2>

      <ul>
      <% @event_pricing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
